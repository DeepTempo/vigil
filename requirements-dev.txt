# Development / CI-only tooling (not required at runtime).
# Install with:  pip install -r requirements-dev.txt

# Enforces the #413 LLM-provider import boundary (see .importlinter).
import-linter>=2.13
