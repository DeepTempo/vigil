name: compose

# Serial over an authored order. There is no decider and no roster: the playbook
# already named every step, so a lead would only re-read what it says.
dispatch:
  mode: serial
  fan_out_over: phases

digest:
  # How many completed steps a later one is shown. A playbook's last step usually
  # writes up the ones before it, so this is the only window that matters here.
  phase_window: 12

# No roles. Which agent runs a step, what it is told and what it may call are all
# resolved at run start, because an agent's prompt and grants are runtime state.
roles: {}
